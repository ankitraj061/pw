/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'ik.imagekit.io','static.pw.live','media.licdn.com'],
  },
};

module.exports = nextConfig;
